
#include <stdio.h>
#include <stdlib.h>
#include <limits.h> 

int check(int arr[],int n){
     int max=0;
     int sum=0;
   lgorith,m
    for(int r=0;r<n;r++){
         sum+=arr[r];
        if(max<sum)max=sum;
        if(sum<0)sum=0;
     }
  printf("\n%d",max);
    return 0;
}
int solve(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
   
    
  check(arr,n);
   
   return 0;
}

int main()
{
    solve();
    printf("\nHello World");

    return 0;
}
