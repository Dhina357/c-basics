#include <stdlib.h>
#include <stdio.h>
#include <limits.h>
#define max 1000001
int check(int* arr1,int n){
    int *com=(int*)calloc(max,sizeof(int));
    int *com1=(int*)calloc(n,sizeof(int));
for(int i=n-1;i>=0;i--){
    com[arr1[i]]++;
    
}
int count=0;
for(int i=0;i<n;i++){
if(com[arr1[i]]>1&&com1[arr1[i]]==0){
     printf("%d ",arr1[i]);
    com1[arr1[i]]=1;
    count=1;
}}
if(!count)printf("no......");
 
    return 0;
   
}
//int compare(const void* a, const void* b) {
//	return (*(int*)a-*(int*)b);

int solve() {
	int n;
	scanf("%d",&n);
	int *arr1=(int*)calloc(n,sizeof(int));
	for(int i=0; i<n; i++) {
		scanf("%d",&arr1[i]);
	}
	/*int m;
	scanf("%d",&m);
	int *arr2=(int*)calloc(m,sizeof(int));
	for(int i=0; i<m; i++) {
		scanf("%d",&arr2[i]);
	}
	*/
	check(arr1,n);
	return 0;
}
int main()
{
	solve();
	printf("\nHello World");

	return 0;
}